// @flow

import Frecency from '../../src/browser';

describe('frecency', () => {
  describe('#save', () => {
    it('should work.', () => {
      const a: number = 99;
      a.toFixed(5);
      expect(a).toBe(99);
    });
  });
});
